apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: masternode-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/preserve-host: "true"
spec:
  rules:
  - http:
      paths:
      - backend:
          serviceName: masternode
          servicePort: 7557
        path: /internal/multichain
