apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app.kubernetes.io/name: sensrnet
  app.kubernetes.io/component: multichain-node

resources:
  - deployment.yaml
  - ingress-tcp.yaml
  - ingress.yaml
  - pvc.yaml
  - service.yaml
