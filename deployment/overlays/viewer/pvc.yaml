apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: multichain-node-pvc
spec:
  storageClassName: managed-premium-retain
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
